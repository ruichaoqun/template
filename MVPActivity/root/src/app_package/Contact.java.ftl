package ${packageName};

import ${applicationPackage}.base.mvp.IBasePresenter;
import ${applicationPackage}.base.mvp.IBaseView;

/**
 * @author Rui Chaoqun
 * @date :${.now}
 * description:MVP 模板自动生成
 */
public interface ${contackClass} {
    interface View extends IBaseView{

    }

    interface Presenter extends IBasePresenter<View>{
  
    }
}
